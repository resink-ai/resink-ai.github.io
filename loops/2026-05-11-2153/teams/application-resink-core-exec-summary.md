---
layout: default
title: application-resink-core Exec Summary — 2026-05-11-2153
date: 2026-05-11
status: active
type: exec-summary
loop: 2026-05-11-2153
owner: teams/application/resink-core
---

<!-- original-frontmatter:
  type: exec-summary
  owner: teams/application/resink-core
  date: 2026-05-11
  status: active
  loop: 2026-05-11-2153
  links: parent: board/okrs/2026-05-11-2153-ceo-brief.md
-->
{% raw %}

# Resink Core Exec Summary — 2026-06-13

**Headline.** ADR-2026-05-16-001 step 2 closed — supervisor dlopen swap full execution against AE's template. New in-tree `crates/nanofab-plugin-dim-user/` workspace member (manual slot-fill of AE's `scd2_maintainer/lib.rs.tmpl` against the `dim_user` schema) builds as a `cdylib` at `target/release/libnanofab_plugin_dim_user.{so,dylib}` (455 KB on macOS) and exports `nanofab_node_new` / `nanofab_node_process` / `nanofab_node_drop`. New integration test `crates/nanofab-supervisor/tests/dlopen_integration.rs` opens the cdylib via `libloading::Library::new`, invokes `nanofab_node_process` on a known `dim_user` insert event (`user_id="u-001"`, `event_ts=1700000000000`), asserts return code `NANOFAB_NODE_OK = 0`, and cross-checks the wrong-table → `NANOFAB_NODE_BLOCKED = 2` mapping under the dlopen path. Both tests passed on first attempt — the loop-1 shim test pre-locked the symbol shape, so the contract held under real load. `make mvp-loop` is GREEN under both feature configurations: default `static-plugins` (existing baseline) AND new `make mvp-loop-dlopen` (rebuilds supervisor under `--no-default-features --features dlopen-plugins` plus the in-tree plugin cdylib). `verdict.json` shape is byte-identical across both runs (`overall_pass: true`, `engine_version: 0.3.0`, both per-dim `pass: true`, `mismatch_count: 0`). Step 3 (hot-swap correctness test) targets loop+1 (= 2026-06-20) per the existing multi-loop plan; tenant-isolation invariant held; one orthogonal concern carries — workspace promotion 5th-loop carry, now 6th by close (see § Asks).

## KR outcomes

| KR | Status | Note |
|---|---|---|
| KR1.1 (built plugin artifact) | **PASS** | New workspace-member crate `crates/nanofab-plugin-dim-user/` (`Cargo.toml` `crate-type = ["cdylib", "rlib"]`, zero external deps preserving codegen template's "dep-free" invariant). `cargo build --release -p nanofab-plugin-dim-user` produces `target/release/libnanofab_plugin_dim_user.dylib` (455 KB); `nm -gU` confirms `_nanofab_node_new`, `_nanofab_node_process`, `_nanofab_node_drop` exported. |
| KR1.2 (dlopen integration test) | **PASS** | `crates/nanofab-supervisor/tests/dlopen_integration.rs` (200+ lines, `#[cfg(feature = "dlopen-plugins")]`). Two tests: `dlopen_load_invoke_drop_dim_user_insert` (raw libloading + symbol lookup + invocation) and `plugin_node_wrapper_round_trips_dim_user_insert` (drives the supervisor's safe `PluginNode` through the same flow). Both pass on first attempt; both assert `NANOFAB_NODE_OK = 0` for the dim_user insert and `NANOFAB_NODE_BLOCKED = 2` for the wrong-table case. |
| KR1.3 (cargo build + test green under both features) | **PASS-with-addendum** | All four required commands exit 0: `cargo build --no-default-features --features static-plugins -p nanofab-supervisor` → 0; `cargo build --no-default-features --features dlopen-plugins -p nanofab-supervisor` → 0; `cargo test --no-default-features --features static-plugins -p nanofab-supervisor --bins` → 5/5 pass; `cargo test --features dlopen-plugins -p nanofab-supervisor` → all green (5/5 bin + 2/2 lib + 2/2 integration; 1 ignored release-only). **Addendum:** mutual-exclusion gate added to `plugin_loader.rs` so `cargo test --features dlopen-plugins` (which auto-activates default `static-plugins` too) compiles cleanly — dlopen wins when both are on. No behavior change for the documented `--no-default-features --features X` invocations. |
| KR1.4 (`make mvp-loop` GREEN under both modes) | **PASS** | `synthetic_tenants/closed_loop_v0/Makefile` adds new `mvp-loop-dlopen` target (chains `orchestrate → build-rust-dlopen → publish-dag → run-supervisor → verdict`). Both modes green-bar with byte-identical `verdict.json`: `overall_pass: true`, `engine_version: 0.3.0`, `dim_user.pass: true / mismatch_count: 0`, `dim_account.pass: true / mismatch_count: 0`. Documented in `docs/user-guide.md` § "Plugin loading modes" (~30 lines). The supervisor's `--mode=sim` runtime path still uses `nodes::{user,account}` adapters statically; the dlopen `PluginNode` is exercised by the integration test (the runtime-path swap is the follow-up after step 3 hot-swap correctness lands). |
| KR1.5 (slippage record updated) | **PASS** | Slippage record in `teams/application/resink-core/status.md` § "ADR-2026-05-16-001 multi-loop plan slippage record": step 1 done at 2026-05-30 (no slippage); step 2 done at 2026-06-13 (one-loop slippage from original 2026-06-06 schedule, scaffold landed at 2026-06-06 + full execution closed this loop); step 3 → 2026-06-20 (joint AE + resink-core, per existing schedule). Lives in team status.md per ADR-2026-05-30-002's loop+N convention. |
| KR1.6 (`docs/architecture.md` § Named deviations) | **PASS** | ABI Option A entry rewritten: opens with "step 2 closed; hot-swap test pending loop+1 (2026-06-20)"; surfaces `--features dlopen-plugins` as the canonical going-forward invocation; lists every load-bearing file (`crates/nanofab-supervisor/Cargo.toml`, `plugin_loader.rs`, `nanofab-plugin-dim-user/`, `tests/dlopen_integration.rs`, `Makefile`'s `mvp-loop-dlopen` target). The carried-from-loop-1 deferred flips for `dim_account` isomorphic-shape lift + `claude --bare` reintroduction were already absent from § "Named deviations" at start-of-loop (the 2026-06-06 work touched their entries in-place); no orphan carry-from-loop-1 lines remain. |
| KR1.7 (tenant-isolation invariant) | **PASS** | `grep -nrE "(resink|nanofab|acme\.ai)" /Users/shijinglu/Workspace/resink.ai/newbase/org-os/` returns exactly one line — pre-existing `acme.ai` placeholder at `org-os/conventions.md:107`. No resink-core writes leaked under `org-os/`. |

**Tally.** 6/7 PASS, 1/7 PASS-with-addendum (KR1.3 mutual-exclusion gate), 0/7 failed, 0/7 deferred. The dlopen step 2 deliverable is a clean close — every code-bearing KR landed and the orthogonal workspace-promotion carry is the only outstanding loop-shape concern (per OKR opportunistic-only framing, no penalty).

## Shipped this loop

- **`crates/nanofab-plugin-dim-user/Cargo.toml`** — new workspace-member crate, `cdylib + rlib` crate-type, zero external deps (preserves codegen template's "dep-free" invariant); package description names ADR-2026-05-16-001 step 2 as the governing decision.
- **`crates/nanofab-plugin-dim-user/src/lib.rs`** — 1078 lines; manual slot-fill of `scd2_maintainer/lib.rs.tmpl` against `dim_user` schema (`user_id` PK; `email` non-null + `country` nullable payload; node struct `DimUserScd2Maintainer`, version 1). Byte-equivalent to the orchestrator's per-tenant codegen output under `synthetic_tenants/closed_loop_v0/workspace/nodes/dim_user_scd2/src/lib.rs`.
- **`Cargo.toml` (workspace root)** — `crates/nanofab-plugin-dim-user` added to `members = [...]`; co-existence note explains its relationship to the per-tenant codegen-output crate under `synthetic_tenants/.../workspace/nodes/`.
- **`crates/nanofab-supervisor/src/lib.rs`** — new file, exposes `pub mod plugin_loader` so integration tests can import `nanofab_supervisor::plugin_loader::{PluginNode, NANOFAB_NODE_*}`. Bin and lib targets compile the same `plugin_loader.rs` source twice (once each); behaviorally equivalent.
- **`crates/nanofab-supervisor/src/plugin_loader.rs`** — `static-plugins` `mod imp` block re-gated to `#[cfg(all(feature = "static-plugins", not(feature = "dlopen-plugins")))]` (mutual-exclusion: dlopen wins when both features on, so `cargo test --features dlopen-plugins` doesn't trip the duplicate-`mod imp` E0428). The corresponding `#[cfg(...)]` on `static_plugins_load_is_noop_stub` test mirrored.
- **`crates/nanofab-supervisor/tests/dlopen_integration.rs`** — new file, 200+ lines, `#[cfg(feature = "dlopen-plugins")]`. Two tests: `dlopen_load_invoke_drop_dim_user_insert` and `plugin_node_wrapper_round_trips_dim_user_insert`. Test setup invokes `Command::new(env!("CARGO")).arg("build").arg("--release").arg("-p").arg("nanofab-plugin-dim-user")` so the artifact rebuilds idempotently per run; artifact path discovery walks up from `CARGO_MANIFEST_DIR` to `target/release/` (platform-aware filename: `.dylib` on macOS, `.so` on Linux, `.dll` on Windows).
- **`synthetic_tenants/closed_loop_v0/Makefile`** — new `.PHONY: mvp-loop-dlopen build-rust-dlopen` targets. `build-rust-dlopen` invokes `cargo build --release --no-default-features --features dlopen-plugins -p nanofab-supervisor` plus `cargo build --release -p nanofab-plugin-dim-user`; `mvp-loop-dlopen` chains `orchestrate → build-rust-dlopen → publish-dag → run-supervisor → verdict`.
- **`docs/user-guide.md`** — new "Plugin loading modes" subsection (~30 lines) documenting `static-plugins` vs `dlopen-plugins`, the `make mvp-loop-dlopen` invocation, and the explicit `cargo build` recipes for each feature configuration.
- **`docs/architecture.md` § "Named deviations"** — ABI Option A entry rewritten per KR1.6.

## Cross-team coordination

- **From AE (loop-1's ADR-2026-05-16-001 step 1 deliverable):** the template extension at `repos/resink-ai/resink-marketplace/plugins/nanofab/skills/codegen-scd2-node/templates/scd2_maintainer/lib.rs.tmpl:451–549` was the **load-bearing input** this loop. The three exported symbols (`nanofab_node_new`, `nanofab_node_process`, `nanofab_node_drop`) and the four `NANOFAB_NODE_*` status code constants were consumed without contract revision — the `status_codes_match_ae_template` test from loop-1 pre-locked the integer wire values, and the integration test confirmed the symbols resolve byte-for-byte against the built cdylib. **No template revision needed; no addendum to ADR-2026-05-16-001 §1.** AE's review-ack of resink-core's consumption is the lightest sufficient handshake.
- **From sim-farm (engine 0.3 verdict shape preservation):** verdict.json shape is byte-identical across both feature configurations (`overall_pass: true`, both per-dim `pass: true`, `engine_version: 0.3.0`, both `mismatch_count: 0`). The sim-farm engine 0.3 contract is unaffected by the supervisor's plugin-loading mechanism — no coordination needed beyond sim-farm's paused-team review-ack of the dlopen swap landing GREEN.
- **From DE (this loop's new convention):** no consumption changes this loop on resink-core's side. The orchestrator already consumes the per-dim schema-JSON shape via `--schema-ref` (settled at 2026-06-06); DE's new canonical convention at `teams/platform/data-engineering/conventions/dim-schema-json.md` is a documentation move, not a contract revision. Resink-core ack the convention shape (matches what the orchestrator already writes); no follow-up cross-team request from us this loop (additive fields like `is_scd2`, `compaction_strategy`, `column_types` route through the standard request mechanism if/when the orchestrator surfaces a need).
- **From DevOps (this loop's chart README refresh):** the small Prerequisites refresh (one-line policy note + ADR-2026-06-06-001 forward-link) at `repos/resink-ai/resink-core/deploy/charts/nanofab-supervisor/README.md` is acknowledged. The edit is non-load-bearing for chart functionality; no counter-proposal.

## Workspace promotion

Still blocked — **sixth-consecutive carry** (was 5th-loop at start; closes as 6th). `git ls-remote https://github.com/resink-ai/resink-core.git` returns "Repository not found" — the canonical evidence; the `resink-ai/resink-core` GitHub remote still does not exist on GitHub. The in-loop board-action ticket `board/actions/2026-06-06-001-create-resink-core-github-remote.md` (P1, `due: 2026-06-13`) stays `status: open` — its forcing function fired this loop but the remote was not created mid-loop. Per the OKR's opportunistic-only framing and the brief's standing answer, no loop penalty applies. **The brief's Out-of-scope section names 5 downstream blockers** (workspace promotion itself; ADR-2026-05-16-001 step 2 commit graph friction; step 3 fixture history friction; HTML capabilities report's in-tree paths citation; the new GitBook publishing path that lands this loop) — increasing by 1 each loop. We did NOT attempt promotion; the `git ls-remote` evidence is the authoritative blocker.

## Asks for the CEO consolidation

- **Workspace promotion to a real git submodule — sixth-consecutive defer; carry retro escalation flag.** The `resink-ai/resink-core` GitHub remote still does not exist (`git ls-remote` evidence above). The in-loop board-action ticket `board/actions/2026-06-06-001-create-resink-core-github-remote.md` stays `status: open`. **Now blocking 5 named downstream items per the brief's Out-of-scope section** (workspace promotion + step 2 commit graph + step 3 fixture history + HTML capabilities report path-update + GitBook publishing's resink-core docs tree integration). The 2026-06-20 retro should propose a different escalation mechanism if the carry persists (e.g., the brief generates the `gh repo create` command and asks the human to run it via the `! prefix` mechanism in the conversation, rather than relying on out-of-loop human action). **Owner: board / CEO / retro.**
- **Hot-swap correctness test (ADR-2026-05-16-001 step 3) — loop+1 coordination with AE.** Joint AE + resink-core deliverable next loop (= 2026-06-20). Resink-core's contribution: extend `dlopen_integration.rs` to load v1 of the plugin, invoke once, then load v2 of the plugin (deliberate logic change in the maintainer's payload-handling), invoke against the same event, assert the diff is observable in the output state. AE's contribution: template-level v1/v2 fixture (a deliberate logic change between v1 and v2 of the same `dim_user_scd2` node fixture). **Ask:** the next CEO brief schedules step 3 jointly under O3 + an AE-active objective; resink-core re-activates AE on a single light deliverable.

## Tenant-isolation invariant

**Held.** Dry-run command: `grep -nrE "(resink|nanofab|acme\.ai)" /Users/shijinglu/Workspace/resink.ai/newbase/org-os/`. Result: one match — `org-os/conventions.md:107: placeholder names like 'acme.ai'.` — pre-existing placeholder. No resink-core writes leaked under `org-os/`. All edits landed under `repos/resink-ai/resink-core/` (new plugin crate + supervisor lib.rs + plugin_loader.rs gate change + integration test + Makefile + both docs) and `teams/application/resink-core/` (status.md + this exec summary + the 2026-06-13 OKR). Per KR1.7, invariant satisfied for the loop.
{% endraw %}
