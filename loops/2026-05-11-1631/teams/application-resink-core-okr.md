---
layout: default
title: application-resink-core OKR — 2026-05-11-1631
date: 2026-06-06
status: active
type: okr
loop: 2026-05-11-1631
owner: teams/application/resink-core
---

<!-- original-frontmatter:
  type: okr
  owner: teams/application/resink-core
  date: 2026-06-06
  status: active
  loop: 2026-05-11-1631
  links: parent: board/okrs/2026-05-11-1631-ceo-brief.md
-->
# Resink Core OKR — 2026-06-06

## Context

After the 2026-05-30 documentation loop made every deviation observable, this loop returns resink-core to code-bearing build work. Our single CEO-assigned objective is O2: pre-stage the supervisor dlopen swap (step 2 of ADR-2026-05-16-001's multi-loop plan, executing loop+1) and re-introduce `claude --bare` to the orchestrator now that AE's DISPATCH.md addendum landed. Two named deviations from the carryover ledger close this loop in coordination with peer teams: the `dim_account` isomorphic-shape compromise lifts alongside sim-farm's engine 0.3 schema-aware join columns (O3 in the brief), and workspace promotion attempts only if the board-action ticket creating `resink-ai/resink-core` on GitHub fires in-loop (otherwise it remains a tracked carry per the brief's standing answer). Per the CEO brief, no fixture run with `dlopen-plugins` this loop — AE produces the `.so` template artifact, we integrate it loop+1.

## Objectives

### O1: Pre-stage the supervisor dlopen swap and re-introduce `claude --bare`

source: ceo-brief

Why it matters: This is resink-core's slice of CEO O2 — step 2 of the ADR-2026-05-16-001 dlopen restoration plan (step 1 = AE's template extension this loop; step 2 = our supervisor swap loop+1 = 2026-06-13; step 3 = hot-swap correctness test loop+2 = 2026-06-20). Scaffolding the `libloading::Library` path behind a feature flag now makes loop+1 a thin integration once AE's template `.so` is in hand. In parallel, the `claude --bare` reintroduction closes the orchestrator-side dispatch workaround that has been carrying for three loops (the AE DISPATCH.md addendum at 2026-05-30 documents when `--bare` is safe — env-var auth + CI use `--bare`; developer-laptop OAuth keychain mode drops it). The `dim_account` isomorphic-shape lift and an opportunistic workspace-promotion attempt absorb the remaining named deviations, contingent on sim-farm O3 landing and the board action ticket firing respectively. `make mvp-loop` must remain green throughout.

**Key results**
- KR1.1: `crates/nanofab-supervisor/Cargo.toml` gains two feature flags: `static-plugins` (default on; current Cargo path-dep behavior preserved) and `dlopen-plugins` (default off; `libloading::Library` path). `cargo build` succeeds with each toggled in isolation; the unit test suite passes for both configurations. No code path is unconditionally compiled when one flag is off.
- KR1.2: `crates/nanofab-supervisor/src/plugin_loader.rs` (or equivalent module) gains the conditional load shape: under `static-plugins`, calls the existing path-dep entrypoint unchanged; under `dlopen-plugins`, calls `libloading::Library::new(...)` against a `.so`/`.dylib` path and resolves the `nanofab_node_process` symbol per AE's template export. `dlopen-plugins`-on `cargo test` exercises a no-op shim placeholder (no real plugin artifact this loop — that's loop+1). The actual exported-symbol signature is absorbed from AE's KR1.3 smoke output rather than ADR-2026-05-16-001 §1's recommended form verbatim (per the brief's Risk #1 mitigation; the ADR's recommended form is the starting point).
- KR1.3: `training/orchestrator/src/orchestrator/dispatch.py` re-introduces `--bare` to the `claude` invocation per AE's DISPATCH.md auth-mode rule: env-var-auto-detect path — when `ANTHROPIC_API_KEY` is present in the environment, pass `--bare`; when absent (developer-laptop OAuth keychain mode), drop `--bare`. The detection happens once at dispatch invocation; the decision is logged to `trace.jsonl` for observability. `make mvp-loop` exits 0 with each env state tested locally (one run with `ANTHROPIC_API_KEY` set, one with it unset).
- KR1.4: `dim_account` isomorphic-shape compromise lifted in resink-core's orchestrator once sim-farm engine 0.3 is integratable in-loop (gates on O3 KR3.1–KR3.2). The orchestrator writes a per-dim `schema.json` next to each fixture/output parquet with the agreed shape `{"key_columns": [...], "payload_columns": [...]}`, and passes `--schema-ref` to `sim_farm.diff_scd2 run_diff_multi` paired by argument order with `--fixture`/`--output`/`--dim-table`. The `dim_account` fixture/output parquets stop carrying synthetic `user_id`/`email`/`country` columns; natural `account_id` + `account_type` + `status` shape restored. The deviation entry in `repos/resink-ai/resink-core/docs/architecture.md` § "Named deviations" is updated from carried to closed-this-loop.
- KR1.5: `make mvp-loop` against the two-dim fixture continues to pass under the updated shape: `overall_pass: true`, both per-dim `pass: true`, both `mismatch_count: 0`. The verdict file at `repos/resink-ai/resink-core/synthetic_tenants/closed_loop_v0/workspace/verdict.json` matches the canonical shape from 2026-05-23 (with `engine_version: 0.3.0` from sim-farm O3). Determinism unchanged: `cargo test --release -p nanofab-supervisor --test determinism -- --include-ignored` exits 0.
- KR1.6: Workspace promotion attempted IF the `resink-ai/resink-core` GitHub remote exists at loop start or fires mid-loop from the board-action ticket (P1 in board's O5 KR5.3). If not, no attempt; the blocker is surfaced verbatim on the exec summary § Asks with a cross-link to `board/actions/2026-06-06-001-create-resink-core-github-remote.md`. Per the brief's standing answer, this KR is attempt-not-required; non-firing of the board action does not penalize the loop.
- KR1.7: Tenant-isolation invariant holds — all edits land under `repos/resink-ai/resink-core/`, `teams/application/resink-core/`, and (if promotion fires) the new submodule URL. No writes to `org-os/`.

**Tasks**
- [x] ✅ done — Add `static-plugins` (default on) + `dlopen-plugins` (default off) Cargo feature flags to `crates/nanofab-supervisor/Cargo.toml`; verify both build paths in isolation — owner: teams/application/resink-core. **Evidence:** `[features]` block at `crates/nanofab-supervisor/Cargo.toml` lines 13–28; `cargo build --no-default-features --features static-plugins -p nanofab-supervisor` exits 0 (14.03s); `cargo build --no-default-features --features dlopen-plugins -p nanofab-supervisor` exits 0 (1.06s, pulls in libloading v0.8.9).
- [x] ✅ done — Scaffold `crates/nanofab-supervisor/src/plugin_loader.rs` conditional load against both feature-flag paths; `dlopen-plugins`-on shim against a no-op placeholder — owner: teams/application/resink-core. **Evidence:** `plugin_loader.rs` exports `PluginNode` + four `NANOFAB_NODE_*` status code constants; `mod imp` is `#[cfg(feature = "static-plugins")]` (no-op stub) and `#[cfg(feature = "dlopen-plugins")]` (libloading-driven, resolves `nanofab_node_new` / `nanofab_node_process` / `nanofab_node_drop` symbols). Module declared in `src/main.rs:27`.
- [x] ✅ done with addendum — Absorb AE's actual exported-symbol signature (from O1 KR1.3 smoke) into the `dlopen-plugins` path before declaring KR1.2 — owner: teams/application/resink-core. **Evidence:** signature mirrored from AE's template at `repos/resink-ai/resink-marketplace/plugins/nanofab/skills/codegen-scd2-node/templates/scd2_maintainer/lib.rs.tmpl:451–549` — type aliases `NanofabNodeNewFn`, `NanofabNodeProcessFn`, `NanofabNodeDropFn` in `plugin_loader.rs` reflect the C-ABI exactly. **Addendum:** the four status-code constants (`NANOFAB_NODE_OK=0`, `_PANIC=1`, `_BLOCKED=2`, `_RETRY=3`) are re-exported by the supervisor to break the build dependency on the codegen crate's inlined declarations; a `#[test]` (`status_codes_match_ae_template`) locks the values in.
- [x] ✅ done — Add `cargo test` coverage for both feature-flag configurations; verify suite passes under each — owner: teams/application/resink-core. **Evidence:** `cargo test --no-default-features --features static-plugins -p nanofab-supervisor --bins` -> 5 passed (includes `static_plugins_load_is_noop_stub`); `cargo test --no-default-features --features dlopen-plugins -p nanofab-supervisor --bins` -> 5 passed (includes `dlopen_plugins_load_fails_on_missing_path`).
- [x] ✅ done — Re-introduce `--bare` to `training/orchestrator/src/orchestrator/dispatch.py` with `ANTHROPIC_API_KEY` env-var auto-detect; log the chosen mode to `trace.jsonl` — owner: teams/application/resink-core. **Evidence:** `dispatch.py` `_should_use_bare()` reads `ANTHROPIC_API_KEY` from env; `_log_bare_decision()` appends one JSONL record `{kind: "dispatch.bare_decision", use_bare, env_var, env_var_present, rationale}` per dispatch invocation. `__main__.py` plumbs `trace_path = <workspace>/trace.jsonl` into `dispatch_codegen`. Functional smoke verified both branches (env absent => use_bare=false + OAuth-rationale; env set => use_bare=true + env-var-rationale).
- [⚠️] ⚠️ done with addendum — Run `make mvp-loop` twice locally: once with `ANTHROPIC_API_KEY` set, once unset; verify exit 0 in both — owner: teams/application/resink-core. **Addendum:** the dispatch path is exercised only when the orchestrator runs without `--skip-dispatch` (i.e., `CLAUDE_DISPATCH=1`). The default `make mvp-loop` uses the in-process slot-fill (no claude invocation, no dispatch.bare_decision log). The `_should_use_bare()` logic was unit-tested in both branches in lieu of a full dispatch run, which requires a working `claude` CLI auth. **Default `make mvp-loop` run:** exit 0; verdict.json: `overall_pass: true`, both per-dim `pass: true`, both `mismatch_count: 0`, `engine_version: 0.3.0`.
- [x] ✅ done — Coordinate schema-JSON shape with sim-farm at loop start; confirm path convention — owner: teams/application/resink-core. **Evidence:** schema shape `{"key_columns": [...], "payload_columns": [...]}` matches sim-farm engine 0.3.0's `_load_schema_ref` contract at `sim-farm/sim_farm/diff_scd2.py:310`. Per-dim files live at `synthetic_tenants/closed_loop_v0/sim-farm-schemas/{dim_user,dim_account}.json`.
- [x] ✅ done — Update orchestrator manifest-generation to write per-dim `schema.json` files; pass `--schema-ref` quadruples to sim-farm CLI per argument-order pairing — owner: teams/application/resink-core. **Evidence:** schema files placed statically at `sim-farm-schemas/{dim_user,dim_account}.json` (orchestrator doesn't need to write them — they're contract artifacts, not derived); Makefile `verdict` target passes `--schema-ref $(SCHEMAS_DIR)/dim_<table>.json` paired by argument order with each `--fixture`/`--output`/`--dim-table` triple.
- [x] ✅ done — Drop `dim_account` isomorphic columns; restore natural `account_id`/`account_type`/`status` shape in the fixture and downstream parquets — owner: teams/application/resink-core. **Evidence:** `fixtures/generate.py` `_PARQUET_SCHEMA_ACCOUNT` is `(account_id, account_type, is_current, status, valid_from, valid_to)`; `DIM_ACCOUNT_SCHEMA_JSON` primary_key is `["account_id"]` with columns `account_id`/`account_type`/`status` (all nullable=False); `fixtures/derive_facts.py` emits `fact_account_open` with `(account_id, account_type, event_id, event_ts, op, status)` columns; `manifest.py:_pk_for("dim_account")` returns `["account_id"]`; supervisor `nodes.rs::account` module reads `account_id` from event after, references `row.account_type` / `row.status`, writes parquet with the natural-column schema.
- [x] ✅ done — Pair-test end-to-end with sim-farm O3 KR3.5; verify `make mvp-loop` green under engine 0.3 with the lifted shape — owner: teams/application/resink-core. **Evidence:** `make mvp-loop` exit 0; verdict.json carries `engine_version: 0.3.0`, `overall_pass: true`, both per-dim `pass: true`, both `mismatch_count: 0` (`workspace/verdict.json`).
- [ ] ⚠️ deferred — Update `docs/architecture.md` § "Named deviations" entries — owner: teams/application/resink-core. **Status:** the closure of the `dim_account` isomorphic-shape and `claude --bare` deviations will land in the exec-summary phase along with status.md; the architecture.md edit was timeboxed off this build phase to avoid drift while the code was in flight.
- [x] ✅ done — Re-run determinism test (`cargo test --release -p nanofab-supervisor --test determinism -- --include-ignored`) and verify exit 0 — owner: teams/application/resink-core. **Evidence:** `test supervisor_run_is_byte_identical_across_two_runs ... ok` (1 passed; finished in 0.25s).
- [⚠️] ⚠️ deferred — Attempt workspace promotion to git submodule — owner: teams/application/resink-core. **Status:** `git ls-remote https://github.com/resink-ai/resink-core.git` returns "Repository not found"; per the OKR's "attempt-not-required" framing and the brief's standing answer, no penalty. Fifth-loop carry; flagged for retro escalation.
- [x] ✅ done — Tenant-isolation dry-run post-edits; verify no writes under `org-os/` — owner: teams/application/resink-core. **Evidence:** `grep -nrE "(resink|nanofab|acme\.ai)" org-os/` returns one line — `org-os/conventions.md:94: placeholder names like \`acme.ai\`.` — the pre-existing placeholder; no resink-core writes leaked.

## Cross-team asks

- **From `teams/platform/agent-engineering`, by mid-loop (2026-06-10):** the actual exported-symbol signature from AE's O1 KR1.3 smoke (the working `extern "C"` declaration as compiled, not the ADR's recommended form). Per the brief's Risk #1 mitigation, we absorb the actual signature into `plugin_loader.rs`. **If AE encounters a C-ABI design surprise:** AE files an addendum and we mirror it in our scaffold. **Reason:** our KR1.2 cannot finalize until AE's symbol shape is settled.
- **From `teams/application/sim-farm`, by mid-loop (2026-06-10):** engine 0.3 with `--schema-ref` argument plumbed, plus the canonical schema-JSON shape confirmed (per the brief's standing answer: `{"key_columns": [...], "payload_columns": [...]}`). **Reason:** our KR1.4 + KR1.5 gate on sim-farm O3 KR3.1–KR3.2 being integratable end-to-end via `make mvp-loop`.
- **From `board`, opportunistic (no hard date):** if the `resink-ai/resink-core` GitHub remote is created in-loop per the P1 board-action ticket, notify resink-core so we attempt the promotion (KR1.6). **Reason:** the ticket's `due: 2026-06-13` is the forcing function — this loop is opportunistic; no penalty for not firing.

## Risks

- **AE's actual `nanofab_node_process` signature could differ from ADR-2026-05-16-001 §1's recommended form** (status code constants, FFI pointer-lifetime semantics, `NodeCtxC` struct shape). Mitigation: per the CEO brief's Risk #1, we absorb the actual signature from AE's KR1.3 smoke rather than re-litigate the ADR; `plugin_loader.rs` references AE's working artifact, not the ADR text.
- **`claude --bare` reintroduction could regress `make mvp-loop` if env-var auto-detect logic is wrong.** Mitigation: KR1.5 explicitly verifies `make mvp-loop` green under both env states; if either regresses, revert to the previous workaround (drop `--bare` unconditionally) and file an addendum. The DISPATCH.md addendum at 2026-05-30 documents both auth modes; rollback is well-defined.
- **Sim-farm O3 slips past mid-loop and our KR1.4 + KR1.5 cannot land cleanly.** Mitigation: schema-JSON shape is settled at loop start via the brief's standing answer; if engine 0.3 itself slips, our orchestrator changes (write `schema.json`, pass `--schema-ref`) land independently and exercise the legacy 0.2.0 backward-compat path (omitted `--schema-ref` falls back) until engine 0.3 catches up next loop. The `dim_account` lift would then carry one loop further; KR1.5 still verifies green under the 0.2.0 fallback shape.
- **Feature-flag scaffolding leaks runtime behavior under `static-plugins` (the default).** Mitigation: every `dlopen-plugins`-guarded line is behind `#[cfg(feature = "dlopen-plugins")]`; `cargo test` under default features must show no behavioral diff vs the 2026-05-23 baseline (determinism test + verdict shape both unchanged).
- **Workspace promotion attempt (KR1.6) fires partway through the loop while code is in flight.** Mitigation: per the brief's standing answer, the promotion is attempt-not-required; we attempt only if the remote is stable and our in-tree changes are at a checkpoint. If mid-flight, defer promotion to loop+1 without penalty.

## Out of scope this loop

- **`dlopen`-plugins full execution against AE's actual `.so` artifact** — that's loop+1 (= 2026-06-13). This loop scaffolds only; the `dlopen-plugins` cargo-test exercises a no-op shim.
- **Hot-swap correctness test** — ADR-2026-05-16-001 step 3, loop+2 (= 2026-06-20). Far out.
- **Third dim** — not in scope; engine 0.3 unlocks the option but no new dim lands this loop.
- **Modes B / C** — out of scope per long-standing posture.
- **Real Kafka, real KV, multi-tenant deployment** — all unchanged from prior loops.
- **`teams/application/resink-core/resink-core/` symlink cleanup** — non-load-bearing; flagged for retro consideration since 2026-05-23.
- **SRE BLOCKED observable verification** — multi-loop wait on dlopen restoration step 2/3; not this loop.
- **`EnterWorktree` discipline restoration** — situational; we proceed in-place under `repos/resink-ai/resink-core/` unless a worktree becomes load-bearing mid-loop.
- **New ADRs from resink-core** — board owns the 2026-05-30-002 ratification this loop; we have no proposal to add.
